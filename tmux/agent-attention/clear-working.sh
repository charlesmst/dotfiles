#!/bin/bash
# Legacy hook stub — working markers no longer exist.
# The picker now derives agent state from the live process tree.
# Kept so existing Claude/Cursor Stop hook configs don't break.
exit 0
